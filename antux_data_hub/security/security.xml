<odoo>
    <data>

        <record id="group_antux_rrhh_planillas" model="res.groups">
            <field name="name">Responsable de Planillas RRHH</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
        </record>

        <record id="rule_datahub_batch_by_company" model="ir.rule">
            <field name="name">DataHub - Batch por Compañía</field>
            <field name="model_id" ref="model_antux_datahub_batch"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', '=', user.company_id.id)]
            </field>
        </record>

        <record id="rule_datahub_line_by_company" model="ir.rule">
            <field name="name">DataHub - Líneas por Compañía</field>
            <field name="model_id" ref="model_antux_datahub_line"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', '=', user.company_id.id)]
            </field>
        </record>

    </data>
</odoo>
