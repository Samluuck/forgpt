<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- Cron job para envío diario de recordatorios a las 16:30 -->
        <record id="cron_timesheet_reminder" model="ir.cron">
            <field name="name">Recordatorio Diario Timesheet - Portal REGIER</field>
            <field name="model_id" ref="model_timesheet_reminder"/>
            <field name="state">code</field>
            <field name="code">model.send_daily_reminder()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <!-- Ejecutar a las 16:30 de lunes a viernes -->
            <field name="nextcall" eval="(DateTime.now().replace(hour=16, minute=30, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
        
    </data>
</odoo>